<Window x:Class="EcommerceStarter.Upgrader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="EcommerceStarter Upgrader" 
        Height="700" 
        Width="900"
        MinHeight="700"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    
    <Window.Resources>
        <!-- Page transition animation -->
        <Storyboard x:Key="PageFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource BrandPrimaryBrush}"
                Padding="30,20">
            <StackPanel>
                <TextBlock Text="⬆ EcommerceStarter Upgrader"
                          FontSize="24"
                          FontWeight="SemiBold"
                          FontFamily="Segoe UI Emoji, Segoe UI Symbol, Arial"
                          Foreground="White"/>
                <TextBlock Text="Upgrade your existing installation to the latest version"
                          FontSize="13"
                          Foreground="White"
                          Opacity="0.9"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Content Area -->
        <Border Grid.Row="1" Padding="0">
            <Frame x:Name="ContentFrame"
                   NavigationUIVisibility="Hidden"
                   Background="Transparent"/>
        </Border>
    </Grid>
</Window>
